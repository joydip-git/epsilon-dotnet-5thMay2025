namespace DelegateImplemantation
{
    //this delegate can be used to invoke any method whose return type is "bool" and accepts one argument of type "int"
    delegate bool LogicInvoker(int value);
}
